{
  "name": "hl7v2-fhir-gateway",
  "version": "1.0.0",
  "description": "REST gateway that ingests HL7v2 ADT messages, transforms them to FHIR, and persists them to a live FHIR server.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
